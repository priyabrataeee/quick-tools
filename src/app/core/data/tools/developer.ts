import { Tool } from '../../tool.types';

export const DEVELOPER_TOOLS: Tool[] = [
  {
    id: 'json-formatter',
    name: 'JSON Formatter',
    description: 'Format, beautify and validate JSON with 2 or 4 space indentation.',
    category: 'developer',
    icon: 'braces',
    keywords: ['beautify', 'pretty print', 'indent', 'json viewer'],
    added: '2026-01-12',
    popular: true,
    trending: true,
    faqs: [
      { q: 'Is my JSON uploaded anywhere?', a: 'No. Parsing and formatting happen with the browser built-in JSON API on your own device. Nothing is transmitted.' },
      { q: 'Does formatting change my data?', a: 'The formatter preserves key order exactly as written. It only changes whitespace, so the parsed value is identical.' },
      { q: 'What is the maximum size?', a: 'There is no hard limit, but documents above a few megabytes may take a moment because they are processed in your browser.' },
    ],
    about: [
      'JSON (JavaScript Object Notation) is the default data-interchange format of the modern web. Minified JSON is efficient to transmit but painful to read, and a single misplaced comma is enough to break a parser without telling you where.',
      'This formatter re-indents your document with two or four spaces, minifies it back down when you need to ship it, and reports the exact position of any syntax error so you can fix it immediately.',
    ],
  },
  {
    id: 'json-validator',
    name: 'JSON Validator',
    description: 'Check whether a JSON document is valid and see exactly where it breaks.',
    category: 'developer',
    icon: 'check-circle',
    keywords: ['lint', 'syntax check', 'parse error'],
    added: '2026-01-18',
    faqs: [
      { q: 'Which JSON specification is used?', a: 'The validator uses the browser native JSON parser, which implements ECMA-404 / RFC 8259. Trailing commas, comments and single quotes are rejected.' },
      { q: 'Does it validate against a schema?', a: 'No. It validates syntax only. JSON Schema validation is a different problem and is not covered by this tool.' },
      { q: 'What do the line and column mean?', a: 'They give the position of the first character the parser could not accept, counted from the start of your document.' },
    ],
    about: [
      'A validator answers one question quickly: is this document parseable JSON, and if not, where does it go wrong? Rather than guessing at an error such as "unexpected token", this tool maps the failure back to a line and column and shows the surrounding text.',
      'It also reports useful statistics about a valid document - total keys, maximum nesting depth and size - which is handy when auditing an API response.',
    ],
  },
  {
    id: 'json-minifier',
    name: 'JSON Minifier',
    description: 'Strip whitespace from JSON and see how many bytes you saved.',
    category: 'developer',
    icon: 'minimize',
    keywords: ['compress', 'compact', 'shrink', 'bytes'],
    added: '2026-01-18',
    faqs: [
      { q: 'Is minified JSON still valid?', a: 'Yes. Whitespace between tokens carries no meaning in JSON, so removing it produces an equivalent document.' },
      { q: 'Does it compress the data?', a: 'It only removes insignificant whitespace. For real compression, serve the response with gzip or brotli on top of the minified payload.' },
      { q: 'Are keys shortened?', a: 'No. Renaming keys would change the meaning of the document, so the minifier never touches them.' },
    ],
    about: [
      'Minifying JSON removes every byte that a parser ignores: the indentation, the newlines and the spaces after colons and commas. On a deeply indented document this routinely removes a fifth to a third of the payload.',
      'The tool shows the before and after size along with the percentage saved, so you can judge whether the trade-off in readability is worth it for your use case.',
    ],
  },
  {
    id: 'jwt-decoder',
    name: 'JWT Decoder',
    description: 'Decode the header and payload of a JSON Web Token and inspect its claims.',
    category: 'developer',
    icon: 'key',
    keywords: ['json web token', 'bearer', 'claims', 'auth', 'oauth'],
    added: '2026-02-02',
    popular: true,
    faqs: [
      { q: 'Does this verify the signature?', a: 'No. Verification requires the signing secret or public key, and you should never paste a production secret into a web page. This tool decodes only.' },
      { q: 'Is my token sent to a server?', a: 'Never. Decoding is base64url plus a JSON parse, both of which run locally in your browser.' },
      { q: 'Why is my expiry shown as a date?', a: 'The exp, iat and nbf claims are NumericDate values - seconds since the Unix epoch - so they are rendered as readable dates for convenience.' },
    ],
    about: [
      'A JSON Web Token is three base64url-encoded segments joined by dots: a header describing the algorithm, a payload of claims, and a signature. The first two segments are encoded, not encrypted, so anyone holding the token can read them.',
      'This decoder splits the token, decodes both segments and highlights the registered claims - issuer, subject, audience, expiry - converting timestamps into local dates and flagging tokens that have already expired.',
    ],
  },
  {
    id: 'uuid-generator',
    name: 'UUID Generator',
    description: 'Generate cryptographically random v4 UUIDs in bulk.',
    category: 'developer',
    icon: 'fingerprint',
    keywords: ['guid', 'unique id', 'v4', 'random', 'identifier'],
    added: '2026-01-22',
    popular: true,
    faqs: [
      { q: 'Are these UUIDs random enough for production?', a: 'Yes. They come from the browser cryptographically secure random number generator and follow RFC 4122 version 4.' },
      { q: 'What is the chance of a collision?', a: 'A v4 UUID carries 122 random bits. You would need to generate billions of them before a collision becomes remotely likely.' },
      { q: 'Can I get uppercase or braced output?', a: 'Yes, use the format options to switch between lowercase, uppercase, braced Microsoft-style GUIDs, or a plain 32-character hex string.' },
    ],
    about: [
      'A UUID is a 128-bit identifier that can be generated independently on many machines with a negligible chance of collision, which makes it ideal for database primary keys, correlation IDs and idempotency keys.',
      'Version 4 UUIDs are made almost entirely of random bits, with six bits reserved to mark the version and variant. This generator uses the Web Crypto API, so the values are suitable for real use rather than just for mock data.',
    ],
  },
  {
    id: 'regex-tester',
    name: 'Regex Tester',
    description: 'Test regular expressions live with highlighted matches and capture groups.',
    category: 'developer',
    icon: 'regex',
    keywords: ['regexp', 'pattern', 'match', 'capture group', 'replace'],
    added: '2026-02-14',
    trending: true,
    faqs: [
      { q: 'Which regex flavour is this?', a: 'JavaScript / ECMAScript regular expressions, exactly as they behave in the browser and in Node.js.' },
      { q: 'Why does my pattern seem to hang?', a: 'Certain nested quantifiers cause catastrophic backtracking. The tester caps the number of matches it collects, but simplifying the pattern is the real fix.' },
      { q: 'Can I use named capture groups?', a: 'Yes. Named groups are supported and are listed by name in the results.' },
    ],
    about: [
      'Regular expressions are dense, and the fastest way to understand one is to watch it run. This tester compiles your pattern as you type, highlights every match in the sample text and breaks each match into its numbered and named capture groups.',
      'It also includes a replacement preview, so you can confirm that a substitution using $1 style references produces exactly the output you expect before you paste it into your code.',
    ],
  },
  {
    id: 'cron-generator',
    name: 'Cron Expression Generator',
    description: 'Build and explain cron schedules in plain English.',
    category: 'developer',
    icon: 'clock',
    keywords: ['crontab', 'schedule', 'job', 'timer', 'cron parser'],
    added: '2026-03-03',
    faqs: [
      { q: 'Which cron dialect is supported?', a: 'The standard five-field Unix crontab format: minute, hour, day of month, month and day of week.' },
      { q: 'What does the next run list show?', a: 'The next five times the expression will fire, calculated in your local timezone from the current moment.' },
      { q: 'Are step and range values supported?', a: 'Yes. Lists, ranges, steps and combinations of the three all work.' },
    ],
    about: [
      'Cron expressions are compact but easy to misread, and a schedule that is off by one field can mean a job runs every minute instead of once a month. Building the expression from labelled controls removes most of that risk.',
      'This generator produces the five-field expression, describes it in plain English, and previews the next several run times so you can sanity-check the schedule before deploying it.',
    ],
  },
  {
    id: 'base64-encoder',
    name: 'Base64 Encoder & Decoder',
    description: 'Convert text to Base64 and back, with full Unicode support.',
    category: 'developer',
    icon: 'code',
    keywords: ['base64', 'encode', 'decode', 'btoa', 'atob', 'url safe'],
    added: '2026-01-15',
    popular: true,
    faqs: [
      { q: 'Does this handle emoji and accented characters?', a: 'Yes. Text is encoded as UTF-8 first, so any Unicode character round-trips correctly - unlike a raw call to the legacy btoa function.' },
      { q: 'What is URL-safe Base64?', a: 'A variant that replaces the plus and slash characters with hyphen and underscore and drops the padding, so the result can be used in a URL or filename without escaping.' },
      { q: 'Is Base64 encryption?', a: 'No. It is an encoding, fully reversible by anyone. Never use it to protect secrets.' },
    ],
    about: [
      'Base64 represents arbitrary bytes using 64 printable ASCII characters, which lets binary data travel safely through channels that only accept text - email bodies, JSON payloads, data URLs and HTTP headers.',
      'The trade-off is size: the encoded form is roughly a third larger than the input. This tool encodes and decodes in both the standard and URL-safe alphabets and correctly handles multi-byte UTF-8 characters in both directions.',
    ],
  },
  {
    id: 'url-encoder',
    name: 'URL Encoder & Decoder',
    description: 'Percent-encode and decode URLs, query strings and components.',
    category: 'developer',
    icon: 'link',
    keywords: ['percent encoding', 'uri', 'query string', 'escape', 'unescape'],
    added: '2026-01-15',
    faqs: [
      { q: 'What is the difference between the two modes?', a: 'Component mode escapes reserved characters such as ampersand, question mark, slash and equals, which is what you want for a single query value. Full URL mode leaves the URL structure intact.' },
      { q: 'Why is a space sometimes a plus sign?', a: 'Form submissions encode a space as a plus, while general percent-encoding uses %20. The decoder accepts both.' },
      { q: 'Does it handle non-ASCII characters?', a: 'Yes. Characters are encoded as UTF-8 bytes, which is what every modern browser and server expects.' },
    ],
    about: [
      'URLs may only contain a limited set of ASCII characters. Everything else - spaces, ampersands inside a value, non-Latin scripts - must be percent-encoded as its UTF-8 bytes so that servers and proxies parse the address the way you intended.',
      'This tool exposes both encoding levels, so you can escape a single query parameter without destroying the URL around it, and also includes a query-string breakdown that shows each parameter decoded.',
    ],
  },
  {
    id: 'html-entity-encoder',
    name: 'HTML Entity Encoder',
    description: 'Escape and unescape HTML entities to display markup safely.',
    category: 'developer',
    icon: 'file-code',
    keywords: ['escape html', 'entities', 'xss', 'nbsp'],
    added: '2026-02-08',
    faqs: [
      { q: 'Which characters are escaped?', a: 'In basic mode, the five characters that matter for markup safety: ampersand, less-than, greater-than, double quote and apostrophe. Full mode also escapes every non-ASCII character as a numeric entity.' },
      { q: 'Is escaping enough to prevent XSS?', a: 'HTML-escaping untrusted text before inserting it into markup prevents the most common injection, but attribute and script contexts need their own escaping rules.' },
      { q: 'Can it decode named entities?', a: 'Yes. Named entities, plus decimal and hexadecimal numeric references, are all decoded.' },
    ],
    about: [
      'Any time user-supplied text is placed into an HTML document, characters that mean something to the parser have to be replaced with entity references - otherwise a stray angle bracket ends your paragraph and a quote breaks out of an attribute.',
      'This encoder converts text to safe entities and decodes entity-laden markup back to readable text, which is equally useful for pasting code samples into a blog post.',
    ],
  },
  {
    id: 'timestamp-converter',
    name: 'Unix Timestamp Converter',
    description: 'Convert Unix timestamps to human dates and back, in UTC and local time.',
    category: 'developer',
    icon: 'timer',
    keywords: ['epoch', 'unix time', 'milliseconds', 'iso 8601', 'utc'],
    added: '2026-01-28',
    popular: true,
    faqs: [
      { q: 'Seconds or milliseconds?', a: 'Both. The converter detects the magnitude of the number and tells you which unit it assumed, and you can override the choice.' },
      { q: 'What timezone is used?', a: 'Results are shown in UTC and in your local timezone at the same time, so there is no ambiguity.' },
      { q: 'What is the year 2038 problem?', a: 'Systems storing time in a signed 32-bit integer overflow in January 2038. Modern platforms use 64-bit values and are unaffected.' },
    ],
    about: [
      'A Unix timestamp counts the seconds elapsed since midnight UTC on 1 January 1970. It is unambiguous and easy to compare, which is why it appears throughout logs, databases and API payloads - and why it is unreadable at a glance.',
      'This converter translates in both directions, accepts seconds or milliseconds, and displays the result as an ISO 8601 string, a UTC date and a local date, alongside a live clock of the current epoch value.',
    ],
  },
  {
    id: 'hash-generator',
    name: 'Hash Generator',
    description: 'Generate SHA-1, SHA-256, SHA-384 and SHA-512 hashes from text or files.',
    category: 'developer',
    icon: 'hash',
    keywords: ['sha256', 'sha512', 'checksum', 'digest', 'crypto', 'md5'],
    added: '2026-02-20',
    faqs: [
      { q: 'Why is MD5 not offered?', a: 'The Web Crypto API deliberately omits MD5 because it is cryptographically broken. Use SHA-256 for anything that matters.' },
      { q: 'Is SHA-1 safe to use?', a: 'No, not for signatures or security. It is included only for compatibility with legacy systems that still require it.' },
      { q: 'Can I hash a file?', a: 'Yes, drop a file into the tool and its bytes are hashed directly, which is the usual way to verify a download checksum.' },
    ],
    about: [
      'A cryptographic hash reduces any input to a fixed-length fingerprint. The same input always yields the same digest, while any change - even one bit - produces a completely different one, which is what makes hashes useful for integrity checks.',
      'This generator uses the browser native crypto implementation, so the digests are computed by the same audited code your browser uses for TLS, and your input never leaves the page.',
    ],
  },
  {
    id: 'sql-formatter',
    name: 'SQL Formatter',
    description: 'Format SQL queries with consistent keyword casing and indentation.',
    category: 'developer',
    icon: 'database',
    keywords: ['beautify sql', 'query', 'pretty print', 'postgres', 'mysql'],
    added: '2026-03-12',
    faqs: [
      { q: 'Which SQL dialects are supported?', a: 'The formatter is dialect-agnostic. It recognises standard clauses and operators shared by PostgreSQL, MySQL, SQL Server and SQLite.' },
      { q: 'Will it change my query?', a: 'No. Only whitespace and keyword casing change; string literals, identifiers and comments are preserved exactly.' },
      { q: 'Does it handle subqueries?', a: 'Yes. Nested parentheses increase the indentation level, so common table expressions and subqueries stay readable.' },
    ],
    about: [
      'A long query written on one line is almost impossible to review. Putting each major clause on its own line and indenting nested expressions turns the same query into something you can read top to bottom.',
      'This formatter upper-cases keywords, breaks before the major clauses, indents parenthesised subqueries, and leaves your literals and comments untouched.',
    ],
  },
  {
    id: 'xml-formatter',
    name: 'XML Formatter',
    description: 'Pretty-print and validate XML documents with proper indentation.',
    category: 'developer',
    icon: 'file-code',
    keywords: ['beautify xml', 'pretty print', 'soap', 'rss', 'svg'],
    added: '2026-03-12',
    faqs: [
      { q: 'Is the document validated?', a: 'Yes, it is parsed with the browser XML parser, so malformed markup is reported with the parser own error message.' },
      { q: 'Are comments and CDATA preserved?', a: 'Yes. Comments, CDATA sections and processing instructions all survive formatting.' },
      { q: 'Does it validate against a schema?', a: 'No. Only well-formedness is checked; schema validation is out of scope for a browser tool.' },
    ],
    about: [
      'XML remains the backbone of SOAP services, RSS feeds, SVG graphics and countless configuration formats. Machine-generated XML usually arrives on a single line, which hides the structure that makes it useful.',
      'This formatter re-indents each element by nesting depth, collapses text-only elements onto one line to keep the output compact, and reports well-formedness errors with their location.',
    ],
  },
  {
    id: 'yaml-formatter',
    name: 'YAML to JSON Converter',
    description: 'Convert YAML to JSON and back, with syntax checking.',
    category: 'developer',
    icon: 'file-text',
    keywords: ['yml', 'yaml parser', 'json to yaml', 'config', 'kubernetes'],
    added: '2026-03-20',
    faqs: [
      { q: 'Which YAML features are supported?', a: 'The common configuration subset: nested mappings, sequences, scalars, quoted strings, comments, multi-line block scalars and inline flow collections.' },
      { q: 'Are anchors and aliases supported?', a: 'Anchors, aliases and custom tags are not resolved. They are rare in hand-written configuration and are reported rather than silently mangled.' },
      { q: 'Why does indentation matter so much?', a: 'YAML uses indentation for structure, and tab characters are not valid indentation. The converter flags tabs explicitly because they are a very common cause of errors.' },
    ],
    about: [
      'YAML is the configuration language of Kubernetes, GitHub Actions, Docker Compose and much of modern infrastructure tooling. Its indentation-sensitive syntax is friendly to write and unforgiving to get wrong.',
      'This converter parses YAML into JSON so you can see exactly how a tool will interpret your file, and converts JSON back into readable YAML when you need to move in the other direction.',
    ],
  },
];
